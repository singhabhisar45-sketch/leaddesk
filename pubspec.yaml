name: internship_digitalheroes
description: "LeadDesk Mini - Lead capture product built for Digital Heroes Training Task"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.2.1
  go_router: ^14.0.0
  google_fonts: ^6.2.1
  url_launcher: ^6.3.0
  intl: ^0.19.0
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/back.png
